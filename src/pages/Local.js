import React, {PureComponent} from 'react';
import PropTypes from 'prop-types';
import Game from './Game.js';

export default class Local extends PureComponent {
  render() {
    return (
      <Game />
    );
  }
}
